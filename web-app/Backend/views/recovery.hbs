<div class="login-container">
    <div class="login-box">
        <h1>¿Olvidaste tu contraseña?</h1>
        <p>No te preocupes. Ingresa tu correo electrónico y te enviaremos un enlace para restablecerla.</p>
        
        <form id="recoveryForm" class="login-form">
            <div class="form-group">
                <label for="email">Correo Electrónico</label>
                <input type="email" id="email" name="email" placeholder="tu@email.com" required>
            </div>

            <button type="submit" class="btn-login-submit">Enviar Enlace</button>
        </form>

        <div class="login-footer">
            <p>¿Ya recordaste tu contraseña? <a href="/login">Iniciar Sesión</a></p>
        </div>
        
        {{#if message}}
            <p style="color: green; text-align: center; margin-top: 10px;">{{message}}</p>
        {{/if}}
        {{#if error}}
            <p style="color: red; text-align: center; margin-top: 10px;">{{error}}</p>
        {{/if}}
    </div>
</div>